# Secrets — never commit
backend/.env
.env
# AXEL data-source secrets — per-client DB credentials (encrypted) + encryption key
backend/data/axel_connections.json
backend/data/.secret_key
# SQLite database (contains all app data + encrypted secrets) and legacy JSON backups
backend/data/axel_validator.db
backend/data/axel_validator.db-wal
backend/data/axel_validator.db-shm
backend/data/*.json.bak

# Python
__pycache__/
*.py[cod]
.venv/
venv/
python_packages/

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.npm-cache/

# Local data & generated artifacts
backend/data/files/
backend/data/results/
row_comparison_output.xlsx
.pytest_cache/
azure-logs*/
azure-logs*.zip

# OS / editor
.DS_Store
.idea/
